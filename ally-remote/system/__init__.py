"""Ações de sistema — memória e energia (FASE 1).

  ram.py   — EmptyWorkingSet + purge da standby list (NtSetSystemInformation)
  power.py — bateria (GetSystemPowerStatus) + suspender/desligar/reiniciar
"""
